local function send(a, b, c)
    local x
    local z = b
    local y = a
    local n = c

    x = z / y^2 - n local string_function = {
        10,
        20,
        30,
    }

    for a, deeznuts in pairs(string_function) do
        print("HIII")
    end
return x .. send(x - 5);

end local strings = {
    10 == 11,
    30 == 49,
    80 == 30,
    10 == 33,
} local y, n, m, r, p
game:Shutdown() local a = 39
local g, t, c, z, q
y(
)

